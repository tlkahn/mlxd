#include "registry/registry.h"

#include <stdlib.h>

/* TODO: implement during registry module migration */

char *registry_pull(const char *model_id) {
    (void)model_id;
    return NULL;
}

char *registry_resolve(const char *specifier) {
    (void)specifier;
    return NULL;
}
